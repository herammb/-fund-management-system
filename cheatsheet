Initialization:

git init: Initialize a new Git repository in the current directory.
Configuration:

git config --global user.name "Your Name": Set your username globally.
git config --global user.email "youremail@example.com": Set your email globally.
Cloning:

git clone <repository_url>: Clone a remote repository to your local machine.
Basic Workflow:

git add <file>: Add changes to the staging area.
git commit -m "Commit message": Commit staged changes with a message.
git status: Check the status of your working directory.
Branching:

git branch: List all branches in the repository.
git branch <branch_name>: Create a new branch.
git checkout <branch_name>: Switch to a different branch.
git merge <branch_name>: Merge changes from another branch into the current branch.
git branch -d <branch_name>: Delete a branch.
Remote Repositories:

git remote -v: List remote repositories.
git pull: Fetch and merge changes from the remote repository.
git push <remote_name> <branch_name>: Push your local changes to a remote repository.
History and Logs:

git log: Show commit history.
git log --oneline: Show concise commit history.
git diff: Show the differences between your working directory and the last commit.
Undoing Changes:

git reset <file>: Unstage changes.
git checkout -- <file>: Discard changes in the working directory.
Tagging:

git tag <tag_name>: Create a new tag at the current commit.
git tag -a <tag_name> -m "Tag message": Create an annotated tag with a message.
git show <tag_name>: Display information about a tag.
Collaboration:

git fetch: Download remote changes without merging.
git merge origin/<branch_name>: Merge remote changes into your branch.
git pull: Fetch and merge remote changes in one command.

git push origin dev
git add .
git commit -m ""
